name: Code Freeze Check

on:
  pull_request:
    branches:
      - main
    types: [opened, synchronize, reopened, ready_for_review]

jobs:
  code-freeze-check:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Get PR labels
        id: labels
        uses: actions/github-script@v6
        with:
          script: |
            const labels = await github.rest.issues.listLabelsOnIssue({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.payload.pull_request.number,
            });
            const labelNames = labels.data.map(label => label.name);
            core.setOutput("labels", labelNames.join(","));

      - name: Check freeze status
        id: freeze_check
        run: |
          set -euxo pipefail
          echo "🔍 Evaluating freeze conditions..."

          # Pull values from repo variables
          FREEZE_FLAG="${{ vars.FREEZE_FLAG }}"
          START_DATE="${{ vars.FREEZE_START_DATE }}"
          END_DATE="${{ vars.FREEZE_END_DATE }}"
          PR_LABELS="${{ steps.labels.outputs.labels }}"

          echo "PR labels: $PR_LABELS"
          echo "Freeze flag: $FREEZE_FLAG | Start: $START_DATE | End: $END_DATE"

          # Bypass if PR has special label
          if echo "$PR_LABELS" | grep -q "bypass-code-freeze"; then
            echo "🛂 bypass-code-freeze label found — allowed" >> $GITHUB_STEP_SUMMARY
            echo "blocked=false" >> $GITHUB_OUTPUT
            exit 0
          fi

          NOW=$(date -u +"%Y-%m-%d")
          BLOCKED="false"

          if [[ "$FREEZE_FLAG" == "true" ]]; then
            echo "🚫 Code Freeze is active (explicit)" >> $GITHUB_STEP_SUMMARY
            echo "🕒 Freeze window: $START_DATE → $END_DATE" >> $GITHUB_STEP_SUMMARY
            BLOCKED="true"
          elif [[ -n "$START_DATE" && -n "$END_DATE" && ( "$NOW" > "$START_DATE" || "$NOW" == "$START_DATE" ) && ( "$NOW" < "$END_DATE" || "$NOW" == "$END_DATE" ) ]]; then
            echo "🚫 Code Freeze is active (date-based)" >> $GITHUB_STEP_SUMMARY
            echo "🕒 Freeze window: $START_DATE → $END_DATE" >> $GITHUB_STEP_SUMMARY
            BLOCKED="true"
          else
            echo "✅ No active freeze — PR allowed" >> $GITHUB_STEP_SUMMARY
          fi

          echo "blocked=$BLOCKED" >> $GITHUB_OUTPUT

      - name: Fail workflow if blocked
        if: ${{ steps.freeze_check.outputs.blocked == 'true' }}
        run: |
          echo "❌ Code freeze is active — failing workflow"
          exit 1
